# ---- Supabase (client-side) ----
# Only the PUBLIC values belong here — Vite exposes any VITE_* var to the browser.
# Never put the service_role key in the frontend.
# See DATABASE_SETUP.txt for where to find each value.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
