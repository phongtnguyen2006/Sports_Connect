export async function fetchEventComments(selectedCommentEvent) {
    //Gets comments from db by calling the backend api
    console.log(`Fetching event comments.\n event: ${JSON.stringify(selectedCommentEvent)}`);
}